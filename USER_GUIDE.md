# 📖 คู่มือการใช้งาน macrdp ฉบับสมบูรณ์ (Quick & Complete User Guide)

คู่มือฉบับเข้าใจง่าย สำหรับการรีโมตควบคุมเครื่อง Mac จากคอมพิวเตอร์ Windows หรืออุปกรณ์อื่นๆ ด้วยความเร็ว ความลื่นไหล และความปลอดภัยสูงสุด

---

## ⚡ 1. ข้อมูลการเชื่อมต่อด่วน (Quick Connect Info)

| รายการ | ข้อมูลที่ต้องกรอก |
| :--- | :--- |
| **IP Address & Port** | `192.168.1.50:3390` *(ตัวอย่าง—เปลี่ยนเป็น IP ของเครื่อง Mac)* |
| **Username** | ชื่อผู้ใช้ Mac ของคุณ (เช่น `macuser`) |
| **Password** | รหัสผ่านล็อกอินเครื่อง Mac ของคุณ |

---

## 🚀 2. วิธีสั่งเปิดใช้งานบนเครื่อง Mac (How to Start)

เปิดโปรแกรม **Terminal** เข้ามาที่โฟลเดอร์นี้ แล้วใช้คำสั่งเดียว:

```bash
./start.sh
```

Launcher จะเลือกโหมดแนะนำและ build แบบ Release ให้อัตโนมัติเมื่อโค้ดใหม่กว่าโปรแกรม จึงไม่ต้องสั่ง `cargo build` เอง หากต้องการเลือกโหมดอื่น ใช้ `./start.sh lan` (คุณภาพสูงสุดที่ใช้งานได้จริงใน LAN), `./start.sh native` (คมที่สุดแต่เฟรมน้อย) หรือ `./start.sh fast` (หน่วงต่ำที่สุดบน LAN) ส่วน `avc444` เก็บไว้สำหรับวิเคราะห์ปัญหาเท่านั้น เพราะ client ที่ทดสอบยังแสดงสีเพี้ยนรุนแรง

### 🌟 โหมด H.264 Video Stream: `./start-ultimate.sh` (ลื่นเนียนตา 60 FPS)
สูตร All-in-One ลื่นไหล 60 FPS สำหรับจอ 1080p / 2K และการดูวิดีโอ/เลื่อนจอเร็วๆ:
```bash
./start-ultimate.sh
```

### 🏛️ โหมด Native HiDPI Bitmap: `./start-native.sh` (คมและนิ่ง เน้นงานข้อความ)
สูตรนี้จับภาพที่ความละเอียดจริงของจอ Retina และส่งเฉพาะบริเวณที่เปลี่ยน ตั้งไว้ที่ **12 FPS โดยตั้งใจ** เพื่อให้ตัวหนังสือและเส้น UI คม พร้อมลดโอกาสภาพค้างหรือกระตุกเมื่อมีการเปลี่ยนทั้งหน้าจอ เหมาะกับการเขียนโค้ด เอกสาร และ Terminal มากกว่าวิดีโอหรือการเลื่อนจอเร็ว ๆ:
```bash
./start-native.sh
```

นี่คือโหมด **RemoteFX / QOI Tile แบบ Windows-like rendering**: โปรแกรมแบ่งเฉพาะบริเวณที่เปลี่ยนเป็น bitmap tiles แล้วส่งให้ client วาดทันทีโดยไม่ผ่าน H.264 ส่วน codec จริงเลือกอัตโนมัติตามที่ client รองรับ — Windows `mstsc` โดยทั่วไปใช้ RemoteFX, FreeRDP มักเลือก QOIZ/QOI, Windows App บางรุ่นใช้ NSCodec และหากไม่มี codec ที่ใช้ร่วมกันจะ fallback เป็น bitmap/RLE

หากเครือข่ายและเครื่อง client รับไหว สามารถเพิ่มเป็น 15 FPS ได้ด้วย `MACRDP_NATIVE_FPS=15 ./start.sh native` แต่ค่าเริ่มต้น 12 FPS เสถียรกว่า

### 🧪 โหมด AVC444: `./start-avc444.sh` (สำหรับวิเคราะห์เท่านั้น)
โหมดนี้ทดลองใช้ HiDPI และ H.264 แบบ 4:4:4 โดยเข้ารหัส main/auxiliary สองภาพต่อเนื่องผ่าน VideoToolbox session เดียว ค่าเริ่มต้นคือ 10 FPS / 60 Mbps / TCP / all-IDR เพื่อเน้นความเสถียรของ decoder แต่ผลทดสอบจริงบน mstsc build 26100 ยังเป็นภาพสีเทาและสีเพี้ยนรุนแรง จึงไม่ควรใช้ทำงานทั่วไป:
```bash
./start.sh avc444
```
ต้องใช้ client ที่รองรับ RDP 10+ AVC444 หาก client รองรับแค่ AVC420 โปรแกรมจะ fallback เป็น H.264 ปกติให้อัตโนมัติ การแก้ encoder ให้เป็น session เดียว, บังคับ all-IDR, ปรับ chroma/NAL และแก้ขอบเขต region ตาม MS-RDPEGFX ผ่านการทดสอบฝั่ง server แล้ว แต่การทดสอบภาพจริงครั้งสุดท้ายยังเพี้ยนเหมือนเดิม หากต้องการใช้งานจริงให้ใช้ `./start.sh` หรือ `./start.sh lan`; ถ้าต้องการตัวหนังสือคมที่สุดให้ใช้ `./start.sh native`

รายละเอียดรวมของการปรับ performance, quality, การลด copy/allocation, launcher และผลทดสอบทั้งหมดอยู่ที่ [`docs/performance-quality-tuning.md`](docs/performance-quality-tuning.md)

### 📁 Launcher หลัก: `./start.sh` (แนะนำ)
เปิดโหมด Ultimate 60 FPS พร้อมตรวจและ build โปรแกรมให้อัตโนมัติ:
```bash
./start.sh
```

### 🔒 การจำกัดให้เฉพาะ IP ที่กำหนดเข้าได้เท่านั้น (`--allow-ip`):
สามารถต่อท้ายคำสั่งเพื่อบล็อก IP อื่นในวงแลนทั้งหมด ยอมให้เฉพาะเครื่องที่ระบุเชื่อมต่อเข้ามาได้:
```bash
./start-ultimate.sh --allow-ip 192.168.1.20
```
*(ถ้ามีหลายเครื่อง สามารถคั่นด้วยเครื่องหมายจุลภาค เช่น `--allow-ip 192.168.1.20,192.168.1.21`)*

*(หากต้องการข้ามการตรวจ build ชั่วคราว ใช้ `MACRDP_SKIP_AUTO_BUILD=1 ./start.sh`)*

---

## 💻 3. วิธีเชื่อมต่อจากเครื่อง Client (Windows / iPad)

### 🪟 สำหรับเครื่อง Windows (ผ่าน mstsc):
1. กดปุ่ม `Win + R` พิมพ์ **`mstsc`** แล้วกด Enter
2. ช่อง **Computer:** ใส่ IP ของเครื่อง Mac เช่น **`192.168.1.50:3390`**
3. กด **Show Options** (แสดงตัวเลือก) แล้วตั้งค่าแนะนำดังนี้:
   * **แท็บ Display:** แนะนำเลือกขนาดหน้าจอเป็น **`1920 x 1080`** หรือ **`2560 x 1440` (2K)**
   * **แท็บ Experience:** ติ๊กเอา **`Persistent bitmap caching` ออก** (ป้องกันบั๊กภาพค้าง)
4. กด **Connect** แล้วกรอก Username และ Password ของ Mac

> 💡 **แอปแนะนำเพิ่มเติมบน Windows:** หากต้องการลดดีเลย์ให้เหลือน้อยที่สุด แนะนำดาวน์โหลดโปรแกรม **`Thincast Client`** (ฟรี) หรือ **`Windows App`** (จาก Microsoft Store) มาเชื่อมต่อแทน `mstsc` ตัวเก่า

---

## ⌨️ 4. สรุปปุ่มลัดคีย์บอร์ดที่จำเป็น (Keyboard Cheat Sheet)

| สิ่งที่ต้องการทำ | 🪟 กดบนคีย์บอร์ด Windows | 🍎 ผลลัพธ์บน Mac |
| :--- | :--- | :--- |
| **สลับแอป / สลับหน้า Full Screen** | **`Win + Tab`** หรือ **`Alt + Tab`** | สลับแอปและเด้งไปยัง Space/Full Screen นั้นทันที |
| **สลับหน้าต่างในแอปเดิม** | **`Win + \``** หรือ **`Alt + \``** | สลับหน้าต่างของโปรแกรมเดียวกัน |
| **เลื่อนเคอร์เซอร์ ข้ามทีละคำ** | **`Alt + ← / →`** | กระโดดข้ามทีละคำ (เหมือน Ctrl+ลูกศร บน Windows) |
| **คลุมดำ ข้ามทีละคำ** | **`Alt + Shift + ← / →`** | ไฮไลต์เลือกข้อความทีละคำ |
| **ไปต้นบรรทัด / ท้ายบรรทัด (Home/End)** | **`Win + ← / →`** | เลื่อนเคอร์เซอร์ไปหน้าสุด/หลังสุดของบรรทัด |
| **ไปบนสุด / ล่างสุดของเอกสาร** | **`Win + ↑ / ↓`** | เหมือนกด Ctrl+Home / Ctrl+End บน Windows |
| **ลบทีละทั้งคำ (Delete Word)** | **`Alt + Backspace`** | ลบคำข้างหน้าทั้งคำ |
| **ลบทั้งบรรทัด (Delete Line)** | **`Win + Backspace`** | ลบทั้งบรรทัดตั้งแต่เคอร์เซอร์ไปข้างหน้า |
| **คัดลอก / วาง / ตัด / บันทึก** | **`Ctrl + C` / `V` / `X` / `S`** | ใช้งานได้ตามความคุ้นเคยของ Windows |
| **ค้นหา Spotlight** | **`Win + Spacebar`** | เปิดช่องค้นหาของ Mac |
| **รีเฟรชภาพ/เสียงฉุกเฉิน (Resync)** | **`Ctrl + Alt + Shift + R`** | แก้อาการภาพ/เสียงกระตุกโดยไม่ต้องต่อใหม่ |

---

## 📊 5. เลือกระหว่าง 1080p vs 2K vs 4K แบบไหนดี?

* **`1080p` หรือ `2K (1440p)` ที่ 60 FPS (แนะนำที่สุด ⭐):**
  เป็นจุดที่ลงตัวที่สุด ภาพคมกริบ ตัวหนังสือไม่เบลอ การเลื่อนเมาส์และการพิมพ์ตอบสนองไวมาก (ไม่หน่วง) และกินไฟน้อยทั้งสองเครื่อง
* **Native HiDPI Bitmap ที่ 12 FPS:**
  เหมาะเมื่อให้ความสำคัญกับตัวหนังสือคมและภาพนิ่งมากกว่าความลื่น โดย launcher จะใช้ความละเอียด backing-pixel ของจอ Mac แบบ 1:1 และไม่ปล่อยให้ความละเอียดที่ client ขอทำให้ต้องส่งภาพเต็มจอทุกเฟรม

---

## 🛠️ 6. ปัญหาที่พบบ่อยและวิธีแก้ไข (Troubleshooting)

### Q1: Client ขึ้นแจ้งเตือน "Low virtual memory / Low memory condition" แล้วหลุด?
* **สาเหตุ:** เกิดจากโปรแกรม `mstsc` ของ Windows เจอบั๊กแคชวิดีโอค้าง
* **วิธีแก้:**
  1. ในสคริปต์เราใส่ `MACRDP_BLANK_RECOVERY=0` ไว้ป้องกันแล้ว
  2. ใน `mstsc` บน Windows ให้ไปที่แท็บ **Experience** แล้วเอาเครื่องหมายถูกออกจาก **`Persistent bitmap caching`**

### Q2: พับฝาจอ MacBook แล้วเครื่องหลุดการเชื่อมต่อ?
* **วิธีแก้:** ต้อง **เสียบสายชาร์จแบตเตอรี่** ค้างไว้ด้วยเสมอ เป็นเงื่อนไขฮาร์ดแวร์ Clamshell Mode ของ Apple ถ้าไม่เสียบสายชาร์จ Mac จะบังคับ Sleep ทันทีที่ปิดฝาจอ

### Q3: ทำไมพิมพ์รหัสผ่านในหน้าขอสิทธิ์ Admin หรือหน้าจอล็อกไม่ได้?
* **สาเหตุ:** เป็นนโยบายความปลอดภัยสูงสุดของ macOS (Secure Input) ที่ห้ามโปรแกรมจำลองคีย์บอร์ดพิมพ์รหัสผ่านระดับ Root เพื่อป้องกันมัลแวร์แอบขโมยรหัสผ่าน

### Q4: สามารถรีโมตจากนอกบ้าน (นอก Wi-Fi เดียวกัน) ได้ไหม?
* **วิธีที่ปลอดภัยที่สุด:** ติดตั้งแอป VPN ส่วนตัวฟรี เช่น **Tailscale** หรือ **ZeroTier** บนเครื่อง Mac และเครื่อง Windows จากนั้นจะสามารถรีโมตหากันจากที่ไหนก็ได้ในโลกอย่างปลอดภัย 100% (ไม่แนะนำให้ Forward Port เราเตอร์ตรงๆ)
